const apiUrl = import.meta.env.VITE_API_URL;
fetch(`${apiUrl}/jornadas?empleado_id=${employeeId}`, {
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  }
});
